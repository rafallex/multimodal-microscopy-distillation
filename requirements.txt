# ── Reproduce the paper figures locally (CPU-only) ──────────────────────────
# These pins match the environment that produced the committed figures/CSVs.
numpy==2.4.4
pandas==3.0.2
matplotlib==3.10.9

# ── Train a version end-to-end (GPU) ───────────────────────────────────────
# The notebooks under notebooks/ run on Kaggle's free T4 x2 tier, which ships
# torch/torchvision/timm/scikit-learn/Pillow preinstalled. Versions below are
# lower bounds known to work; on Kaggle you do not install these yourself.
torch>=2.0
torchvision>=0.15
timm>=0.9.0              # efficientnet_b0.ra_in1k backbone
scikit-learn>=1.3        # ROC-AUC, train/val splits
Pillow>=10.0             # image loading

# ── Notes ───────────────────────────────────────────────────────────────────
# Local figure reproduction needs only the first block:
#     pip install numpy pandas matplotlib
#     python overleaf-report/figure-sources/build_lb_progression.py
#     python overleaf-report/figure-sources/build_arch_diagram.py
#     python overleaf-report/figure-sources/build_teacher_prob_histogram.py
#
# Full model training is done on Kaggle Notebooks (see README "Reproducing a run").
